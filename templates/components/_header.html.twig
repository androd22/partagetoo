<nav class="navbar navbar-expand-lg py-0">
    <div class="container-fluid px-5">
        <a class="navbar-brand" href="{{ path('default_home') }}"> <img src="{{ asset('img/PartageToo.png') }}" class="w-75  " alt=""></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 fs-5">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('default_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Categories</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {% if is_granted('ROLE_PUBLISHER') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('ad_add') }}">Créer une annonce</a>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>
            <ul class="nav justify-content-end">
                <li class="nav-item">
                    <a class="nav-link fs-5" href="{{ path('app_login') }}"><i class="fa-solid fa-right-to-bracket" style="color: #2f3a50;"></i></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fs-5" href="{{ path('user_register') }}"> <i class="fa-solid fa-user-plus" style="color: #2f3a50;"></i></a>
                </li>

            </ul>
            {#            <form class="d-flex" role="search">#}
            {#                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">#}
            {#                <button class="btn btn-outline-info" type="submit">Search</button>#}
            {#            </form>#}
        </div>
    </div>
</nav>
<div class="container-fluid px-5 bg-warning">
    <nav class="navbar navbar-expand-lg py-0">


        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0 fs-5">

                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href=""></a>
                </li>

            </ul>

        </div>
    </nav>
</div>